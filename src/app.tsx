import { Outlet } from 'react-router-dom'

/* A casca do app, com sidebar, topbar e barra do celular, entra no passo 8.
   Aqui ela é só o lugar onde as telas vão aparecer. */
export function App() {
  return <Outlet />
}
